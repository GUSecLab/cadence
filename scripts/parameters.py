# Global variables for p and k values

"""
defaults for NDSS included:

p_values = [0.55, 0.6, 0.65]
k_values = [2, 4, 6]
buffer_size_values = [500]
message_type = 3
"""

p_values = [0.55, 0.6, 0.65]

k_values = [2, 4, 6] # must be integers

buffer_size_values  = [500]

# 0: DM, 1: Group Message, 2: Bulk DM, 3: Bulk DM Gradual Generation, 4: Bulk DM Gradual Generation Half Timespan 
message_type = 3

